"""Palette synthesis: fuse measured evidence and declared intent into the output views.

`inventory.py` fuses screenshot area-truth with element semantics into `ColorCluster`s;
`usage.py` builds the usage-keyed view; `reconcile.py` pools measured usage against
declared token intent and reports divergences; `roles.py` derives the 60/30/10 roles view.
"""
