"""Color primitives and perceptual matching shared across the pipeline.

`primitives.py` handles parsing and conversion; `match.py` provides the
perceptual sameness/distinctness comparisons the inventory and reconcile
stages cluster with.
"""
